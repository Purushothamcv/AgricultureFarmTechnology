# ============================================
# RENDER DEPLOYMENT - OPTIMIZED FOR 512MB RAM
# ============================================
# Pinned versions to prevent pip backtracking
# and avoid SQLAlchemy dependency resolution issues

# Core Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.0
pydantic[email]==2.7.1
python-multipart==0.0.6

# MongoDB (NO SQL database needed)
pymongo==4.8.0
motor==3.5.1

# Authentication & Security
bcrypt==4.1.2
python-jose[cryptography]==3.3.0
google-auth==2.31.0
google-auth-oauthlib==1.2.1
cryptography==43.0.0

# Data Processing
pandas==2.2.2
numpy==1.26.4
requests==2.32.3

# ML/Prediction Models
scikit-learn==1.5.1
joblib==1.4.2
xgboost==2.1.1

# Image Processing
pillow==10.4.0

# TensorFlow (MINIMAL - lazy loaded, NOT at startup)
# Note: Keras 3.x comes bundled with TensorFlow 2.16+
tensorflow==2.16.1

# AI Features (Groq + LangChain, optional)
# Note: Versions pinned for compatibility
groq==0.9.0
langchain==0.1.20
langchain-groq==0.0.1

# Explicitly pin SQLAlchemy to prevent resolver backtracking
# (pulled in by langchain ecosystem, not used directly)
sqlalchemy==2.0.35

# ============================================
# REMOVED (MongoDB project uses no SQL):
# ============================================
# - sqlmodel, alembic, psycopg2, databases
#
# REMOVED (not needed for runtime):
# - streamlit, matplotlib, seaborn (training only)
# - folium, streamlit-folium (Streamlit UI only)
# - plotly, dash (visualization only)
