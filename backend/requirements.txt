# ============================================
# RENDER DEPLOYMENT - OPTIMIZED FOR 512MB RAM
# ============================================
# Core dependencies (REQUIRED)
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
pydantic==2.5.0
pydantic[email]==2.5.0

# Database
pymongo==4.6.0
motor==3.3.2

# Authentication
bcrypt==4.1.1
python-jose[cryptography]==3.3.0
google-auth==2.25.2
google-auth-oauthlib==1.1.0
cryptography==41.0.7

# Data Processing (LIGHTWEIGHT versions)
pandas==2.1.3
numpy==1.26.2
requests==2.31.0

# ML/Prediction Models (only when needed)
scikit-learn==1.3.2
joblib==1.3.2
xgboost==2.0.2

# TensorFlow (MINIMAL - lazy loaded)
# NOTE: Only imported inside endpoints, not at startup
tensorflow==2.14.0
keras==2.14.0

# Image Processing
pillow==10.1.0

# Frontend Support
python-multipart==0.0.6

# AI Features (optional, only if GROQ_API_KEY exists)
groq==0.4.1
langchain==0.1.0
langchain-groq==0.0.1

# REMOVED (not needed/heavy packages):
# - streamlit (only needed for separate Streamlit app)
# - matplotlib (only for training visualizations)
# - seaborn (only for training plots)
# - folium, streamlit-folium (only for Streamlit UI)
