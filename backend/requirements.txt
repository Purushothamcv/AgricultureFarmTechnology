# ============================================
# RENDER DEPLOYMENT - PRODUCTION STABLE
# ============================================
# Optimized for Render free tier (512MB RAM)
# Minimal dependencies, all versions pinned

# Core Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
uvloop==0.20.0

# Environment & Configuration
python-dotenv==1.0.0
pydantic==2.7.1
python-multipart==0.0.6

# Database (MongoDB - no SQL)
pymongo==4.8.0
motor==3.5.1

# Authentication
bcrypt==4.1.2
python-jose==3.3.0
cryptography==42.0.7
google-auth==2.31.0
google-auth-oauthlib==1.2.1

# Data Processing
pandas==2.2.2
numpy==1.26.4

# HTTP & Utilities
requests==2.32.3
httpx==0.28.1
email-validator==2.3.0

# ML Models (lazy loaded)
scikit-learn==1.7.2
joblib==1.4.2
xgboost==2.1.1
pillow==10.4.0

# AI Integration
groq==0.9.0

# ============================================
# NOTES
# ============================================
# - NO TensorFlow (causes Render crashes)
# - NO SQLAlchemy (not used, causes backtracking)
# - NO LangChain (use Groq directly instead)
# - All model loading is lazy (on-demand)
# - All service imports have try/except fallbacks
# 
# Environment variables (set in Render dashboard):
#   GROQ_API_KEY=sk-...
#   LOW_MEMORY_MODE=true
#   MONGODB_URL=mongodb+srv://...
#   GOOGLE_CLIENT_ID=...
#   GOOGLE_CLIENT_SECRET=...
